# echo "hi"
# -d If directory exists
# -f if file exists
# $? to check exit code of previous command 0[success] , nonZero[err]
# $0 name / $1/$2/$3 Arguments
# variable = $1 , can take input as argument into the script
# find <dir/file> -type f/d , for finding file or dir  
# wc , to count words lines [ -l ]
# -du > disk usgae 
# -du -h > human readable [KB/MB/GB]
#
#
#
#
#
#
#
#
#
#
 
